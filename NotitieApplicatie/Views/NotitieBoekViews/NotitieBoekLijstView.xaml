<UserControl x:Class="NotitieApplicatie.Views.NotitieBoekLijst"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NotitieApplicatie.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Titel}" FontSize="40" FontWeight="Bold" Grid.Row="0"></TextBlock>
        <ListBox ItemsSource="{Binding Notitieboeken}" SelectedItem="{Binding GeselecteerdeNotitieBoek}" Grid.Row="1"></ListBox>
        <Button Content="Nieuwe NotitieBoek" Command="{Binding AddNotitieBoekCommand}" Grid.Row="2"></Button>

    </Grid>
</UserControl>
